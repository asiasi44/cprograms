//WAP to create an array of ten integers using loop
#include<stdio.h>

main(){

    int x[10],i;
    for(i=0;i<10;i++){

        scanf("%d",&x[i]);
        printf("%d",x[i]);
    }
    // printf("%d",&x);
}